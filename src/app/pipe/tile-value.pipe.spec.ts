import { TileValuePipe } from './tile-value.pipe';

describe('TileValuePipe', () => {
  it('create an instance', () => {
    const pipe = new TileValuePipe();
    expect(pipe).toBeTruthy();
  });
});
